Cambios realizados
Se creó y actualizó la imagen oficial de Tryton 7.0 para los servicios tryton-init, tryton-server y tryton-cron.

Se ajustó el archivo docker-compose.yml para usar la imagen oficial tryton/tryton:7.0 en lugar de builds locales.

Se añadieron volúmenes para persistir datos de PostgreSQL (pgdata) y Tryton (tryton-data).

Se montaron los módulos personalizados en la ruta /usr/local/lib/python3.11/dist-packages/trytond/modules/custom dentro de los contenedores.

Se ajustó el archivo de configuración trytond.conf para incluir los módulos personalizados (inventario_basilos y basilios_proveedores), agregando correctamente la ruta personalizada en la clave paths.

Se limpiaron los volúmenes de Docker para eliminar datos y configuraciones previas y evitar conflictos.

Se definió un orden correcto de arranque: primero PostgreSQL, luego la inicialización con tryton-init, y finalmente tryton-server y tryton-cron.

Estado actual
Los contenedores se levantan correctamente y los servicios están activos.

Los módulos personalizados se montan y detectan en la ruta esperada dentro del contenedor.

Al ejecutar la inicialización con trytond-admin -d tryton --all -c /etc/trytond.conf, aparece el error:

python
Copiar
Editar
TypeError: expected str, bytes or os.PathLike object, not NoneType
que indica un problema con la detección o carga de alguno de los módulos personalizados.